apply plugin: 'application'

mainClassName = 'com.genymobile.relay.Relay'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    testCompile 'junit:junit:4.12'
}

jar.manifest {
    attributes(
        'Main-Class': mainClassName
    )
}
