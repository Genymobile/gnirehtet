apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    testCompile 'junit:junit:4.12'
}

jar.manifest {
    attributes(
        'Main-Class': 'com.genymobile.relay.Relay'
    )
}
